[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = tests.py test_*.py
addopts = --reuse-db
norecursedirs = frontend media
markers =
  now: marker for tests which are currently developed
